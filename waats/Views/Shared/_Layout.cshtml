

@{
    var fullUrl = Request.Url?.AbsoluteUri;
    var scheme = Request.Url?.Scheme;              // e.g., "http" or "https"
    var host = Request.Url?.Host;                  // e.g., "www.example.com"
    var path = Request.Url?.AbsolutePath;          // e.g., "/home/index"
    var queryString = Request.Url?.Query;

   
       
   
}


<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="cache-control" content="no-cache, must-revalidate, post-check=0, pre-check=0" />
    <meta http-equiv="cache-control" content="max-age=0" />
    <meta http-equiv="expires" content="0" />
    <meta http-equiv="expires" content="Tue, 01 Jan 1980 1:00:00 GMT" />
    <meta http-equiv="pragma" content="no-cache" />
    <title>
        @ViewBag.Title -
    </title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" rel="stylesheet">
    @*@Styles.Render("~/Content/css")
        @Styles.Render("~/Content/datatablescss")
        @Styles.Render("~/Content/toastrcss")
        @Scripts.Render("~/bundles/modernizr")*@
    <link href="https://fonts.googleapis.com/css2?family=Figtree:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.min.css">
    <link href="~/Content/css/dashboard.css" rel="stylesheet" />
    @if (path == "/")
    {
        <link href="~/Content/css/style.css" rel="stylesheet" />
    }

    <link href="~/Content/css/dataTablePages.css" rel="stylesheet" />
    <link href="~/Content/css/responsive.css" rel="stylesheet" />
    <script type="text/javascript"> var baseUrl = "@Url.Content("~")";</script>
    @*@{
            var scheme = Context.Request.Scheme;             // e.g., http or https
            var host = Context.Request.Host.Value;           // e.g., www.example.com
            var path = Context.Request.Path;                 // e.g., /home/index
            var queryString = Context.Request.QueryString;   // e.g., ?id=1
        }

        <p>Scheme: @scheme</p>
        <p>Host: @host</p>
        <p>Path: @path</p>
        <p>Query String: @queryString</p>*@
</head>

<body>
    @if (path == "/")
    {
        <!--translation dropdown-->
        <div class="translation-wrapper">
            <div class="container-fluid">
                <div class="d-flex justify-content-between">
                    <div>
                        <img src="~/Content/images/ADHD-Logo.png" class="img-fluid logo-survey" />
                    </div>
                    <div>
                        @Html.Partial("_Translator")
                    </div>
                </div>
            </div>
        </div>
    }
    @*@{ if (User.Identity.IsAuthenticated)
        {*@
    @if (path == "/")
    {
        <div class="total-wrapper">
            <nav class="navbar navbar-expand-lg main-menu">
                <div class="container-fluid p-0">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link active" href="@Url.Action("SchedulingTasksList", "UProfile")">
                                    <i class="fa-regular fa-calendar-days pe-2" aria-hidden="true"></i>Scheduling Tasks
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("MindMeditation", "UProfile")">
                                    <i class="fa-solid fa-spa pe-2" aria-hidden="true"> </i>Mindfullness Meditation
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("brainFitness", "UProfile")">
                                    <i class="fa-solid fa-brain pe-2"></i>Brain Fitness
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("SpottingTriggers", "UProfile")">
                                    <i class="fa-regular fa-life-ring pe-2" aria-hidden="true"></i>Spotting Triggers
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("MemoryMaker", "UProfile")">
                                    <i class="fa-solid fa-handshake pe-2" aria-hidden="true"> </i>Memory Maker
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("MindfullAttention", "UProfile")">
                                    <i class="fa-solid fa-leaf pe-2" aria-hidden="true"></i>Mindfull Attention

                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("GratitudeJournal", "UProfile")">
                                    <i class="fa-solid fa-hand-holding-heart pe-2" aria-hidden="true"></i>Gratitude Journal
                                </a>
                            </li>
                            @*</ul>*@
                            @*<ul class="navbar-nav d-flex flex-row">*@
                            <li class="nav-item text-center">
                                @Html.Partial("_LoginPartial")
                            </li>
                            @*</ul>*@
                    </div>
                </div>
            </nav>
        </div>
    }
    else
    {
        <div class="container-fluid">
            <div class="row">
                <!-- Sidebar -->
                <div class="col-md-3 col-lg-2 sidebar">
                    <div class="text-left mb-4">
                        <img src="~/Content/images/ADHD-Logo.png" class="img-fluid w-75" />
                    </div>
                    <div class="d-flex sidebar-wrapper">
                        <nav class="nav flex-column w-100">
                            <a class="nav-link active" href="@Url.Action("Index", "UProfile")"><i class="fa-solid fa-gauge-high"></i>Dashboard</a>
                            <a class="nav-link" href="@Url.Action("SchedulingTasksList", "UProfile")"><i class="fa-regular fa-calendar-days"></i>Schedule</a>
                            <a class="nav-link" href="@Url.Action("MindMeditation", "UProfile")"><i class="fa-solid fa-spa"></i>Mindfulness Meditation</a>
                            <a class="nav-link" href="@Url.Action("brainFitness", "UProfile")"><i class="fa-solid fa-brain"></i>Brain Fitness</a>
                            <a class="nav-link" href="@Url.Action("SpottingTriggers", "UProfile")"><i class="fa-regular fa-life-ring"></i>Spotting Triggers</a>
                            <a class="nav-link" href="@Url.Action("MemoryMaker", "UProfile")"><i class="fa-solid fa-handshake"></i>Memory Maker</a>
                            <a class="nav-link" href="@Url.Action("MindfullAttention", "UProfile")"><i class="fa-solid fa-leaf"></i>Mindful Attention</a>
                            <a class="nav-link" href="@Url.Action("GratitudeJournal", "UProfile")"><i class="fa-solid fa-hand-holding-heart"></i>Gratitude Journal</a>
                        </nav>
                        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
                        {
                            @Html.AntiForgeryToken()
                            <nav class="nav flex-column w-100">

                                <a class="nav-link" href="javascript:document.getElementById('logoutForm').submit()"><i class="fa-solid fa-arrow-right-from-bracket "></i> Logout</a>
                            </nav>
                        }
                    </div>

                </div>
                <!-- Main Content -->
                <div class="col-md-9 col-lg-10 p-4 main-content">
                    <div class="stat-tabs">
                        <ul class="nav nav-pills nav-tabs border-0" id="myTab" role="tablist">
                            <li class="nav-item" role="presentation">
                                <button class="nav-link text-secondary active" id="sectionZero-tab" data-bs-toggle="tab"
                                        data-bs-target="#sectionZero-tab-pane" type="button" role="tab"
                                        aria-controls="sectionZero-tab-pane" aria-selected="false">
                                    <i class="fa-solid fa-list-check"></i> My Task
                                </button>
                            </li>
                            <li class="nav-item" role="presentation">
                                <button class="nav-link text-secondary disabled" aria-disabled="true" id="sectionOne-tab"
                                        data-bs-toggle="tab"
                                        data-bs-target="#sectionOne-tab-pane" type="button" role="tab"
                                        aria-controls="sectionOne-tab-pane" aria-selected="false">
                                    <i class="fa-solid fa-chart-line"></i> My Statistics
                                </button>
                            </li>
                        </ul>
                        <!-- Button trigger modal -->
                    </div>
                    @RenderBody()
                </div>
            </div>
        </div>
    }
    @* }
        }*@


    <!-- Navbar -->
    <!-- Content -->
    @if (path == "/")
    {
        <div class="total-wrapper pt-0">
            <div class="container-fluid bg-whole-wrapper">
                @RenderBody()
            </div>
        </div>
    }
    else
    {

    }
    @Html.Partial("_Modal")
    @*@Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/bootstrap")*@



    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/dataTables")
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    @Scripts.Render("~/bundles/toastr")
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl);
            });
        });
    </script>
    <script>

        // Get the current URL path (e.g., /UProfile/Index)
        var currentPath = window.location.pathname;

        // Remove 'active' class from all links
        $(".nav-link").removeClass("active");

        // Add 'active' class to the link that matches the current path
        $(".nav-link").each(function () {
            if ($(this).attr("href") === currentPath) {
                $(this).addClass("active");
            }
        });
        function googleTranslateElementInit() {
            new google.translate.TranslateElement({ includedLanguages: 'en,es,fr,ur,hi', autoDisplay: true, layout: google.translate.TranslateElement.InlineLayout.SIMPLE }, 'google_translate_element');
            ///new google.translate.TranslateElement({ autoDisplay: true, layout: google.translate.TranslateElement.InlineLayout.SIMPLE }, 'google_translate_element');
        }
    </script>

    @RenderSection("scripts", required: false)
    <script type="text/javascript" src="https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
</body>
</html>
@Html.Partial("_Toastr")
